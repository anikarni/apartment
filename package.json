{
  "name": "apartment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
		"start": "reveal-md slides.md --theme own --title 'Móveis, apartamento e doações'",
    "build": "mkdir -p dist && cp -r {screenshots,pictures,theme} dist && cp -r node_modules/reveal.js/{css,js,lib,plugin} dist && reveal-md slides.md --theme own --title 'Móveis, apartamento e doações' --static > dist/index.html",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^0.11.0",
    "reveal-md": "0.0.30"
  }
}
